---
description: HTML 原型/demo 输出目录与文件结构规范
globs: demo文件/**/*.html
alwaysApply: true
---

# HTML Demo 输出规范

在本工作空间中创建或生成 HTML 原型/demo 时，必须遵守以下结构。

## 目录

- 所有 HTML 原型放在 `demo文件/` 下
- 每次新建 demo 使用独立子文件夹，命名格式：`MM-DD-主题`
  - 示例：`demo文件/06-18-提现流程/`
  - 日期为创建当天的月-日，**不带年份**
  - 主题用简短中文或英文 kebab-case，避免空格

## 文件夹内文件

- 放入实际原型 `.html` 文件即可，文件名与主题一致，如 `提现流程.html`
- **不要**自动生成 `index.html`

## 禁止

- 不要在 `demo文件/` 根目录直接新建散落的 `.html`（历史文件除外）
- 不要在项目根目录创建 HTML 原型

## 示例结构

```
demo文件/
└── 06-18-商品搜索弹窗/
    └── 商品搜索弹窗.html
```

## 更新已有 demo

修改已有文件夹内的 HTML 时，保持原文件夹名不变；仅在新主题/新版本 demo 时新建 `MM-DD-主题` 文件夹。
